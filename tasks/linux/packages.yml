---
# Install system packages via APT

- name: Install system packages
  become: true
  ansible.builtin.apt:
    name: "{{ system_packages }}"
    state: present
    update_cache: true
    lock_timeout: 300
