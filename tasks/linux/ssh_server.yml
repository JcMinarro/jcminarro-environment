---
# Install and enable SSH server on Linux

- name: Install openssh-server
  become: true
  ansible.builtin.apt:
    name: openssh-server
    state: present
    update_cache: true
    lock_timeout: 300

- name: Enable and start SSH service
  become: true
  ansible.builtin.service:
    name: ssh
    state: started
    enabled: true
