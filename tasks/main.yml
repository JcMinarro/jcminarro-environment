---
# Tasks file for jcminarro-environment
# This is the main entry point that delegates to OS-specific roles
# DO NOT add OS-specific tasks here - use the appropriate role instead:
# - roles/common/    for OS-agnostic tasks
# - roles/linux/     for Ubuntu/Debian specific tasks
# - roles/macos/     for macOS specific tasks

# This file is intentionally minimal - all actual tasks are in the roles/ directory