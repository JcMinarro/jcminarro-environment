#!/bin/bash
# AppImage wrapper script for {{ item.appimage_alias }}
# Generated by Ansible

exec /opt/appimage/{{ item.appimage_url | basename }} {% if item.appimage_flags is defined %}{{ item.appimage_flags }}{% endif %} "$@"